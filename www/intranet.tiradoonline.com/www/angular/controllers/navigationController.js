tiradoonlineNamespace.controller
(
	'navigationController', function($scope, $http, $location)
	{
		/*
   		$scope.redirectToURL = function(url)
		{
			alert(url);
			$location.path(url);			
		}
		*/	
		//$scope.ValidateLogin();
	}
);